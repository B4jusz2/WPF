<Window x:Class="Macska.SzarmazasAblak"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Származás adatai" Height="240" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Azonosító:" Grid.Row="0" Margin="0,5"/>
        <TextBox x:Name="txtCountryId" Grid.Row="0" Grid.Column="1" Margin="0,5"/>

        <TextBlock Text="Ország:" Grid.Row="1" Margin="0,5"/>
        <TextBox x:Name="txtCountryName" Grid.Row="1" Grid.Column="1" Margin="0,5"/>

        <TextBlock Text="Kontinens:" Grid.Row="2" Margin="0,5"/>
        <TextBox x:Name="txtContinent" Grid.Row="2" Grid.Column="1" Margin="0,5"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Mentés" Width="100" Margin="5" Click="BtnMentes_Click"/>
            <Button Content="Mégse" Width="100" Margin="5" Click="BtnMegse_Click"/>
        </StackPanel>
    </Grid>
</Window>